import { BankPageSkeleton } from "./components/skeletons";

/**
 * Loading state component for the Bank page
 * Displays skeleton UI while the page is loading
 */
export default function BankLoading() {
  return <BankPageSkeleton />;
} 