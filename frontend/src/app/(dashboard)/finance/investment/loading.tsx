import { InvestmentPageSkeleton } from "./components/skeletons";

/**
 * Loading state component for the Investment page
 * Displays skeleton UI while the page is loading
 */
export default function InvestmentLoading() {
  return <InvestmentPageSkeleton />;
} 