model Feature {
  id             Int           @id @default(autoincrement())
  type           FeatureType
  name           String        @default("")

  membershipFeatures MembershipFeature[]
}

enum FeatureType {
  CRYPTO
  EXPENSE
}

