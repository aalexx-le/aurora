apiVersion: v1
kind: Service
metadata:
  name: database
  labels:
    app.kubernetes.io/name: database
    app.kubernetes.io/part-of: xela
spec:
  selector:
    app.kubernetes.io/name: database
  ports:
  - port: 5432
    targetPort: postgres
    name: postgres
  clusterIP: None 